// prisma/schema.prisma
// Schema para Matthew Journal — persistencia REAL cross-device (Neon PostgreSQL).
// Migrado desde localStorage. Usa Prisma client como source of truth.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // directUrl = env("DIRECT_URL")  // opcional: migraciones directas
}

model Brother {
  id        String   @id @default(uuid())
  name      String   @default("Sofía")
  role      String   @default("prima")
  age       String?
  photoUrl  String?
  message   String?
  category  String   @default("primos")
  order     Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([category])
}

model Event {
  id               String   @id @default(uuid())
  title            String
  date             String   // ISO YYYY-MM-DD
  time             String?
  location         String?
  address          String?
  description      String?
  thankYouMessage  String?
  type             String   @default("bautizo")
  photoUrl         String?
  isPublic         Boolean  @default(true)
  isEnabled        Boolean  @default(true)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([id])
}

model Photo {
  id        String   @id @default(uuid())
  src       String   // URL Vercel Blob (no base64 en DB)
  alt       String?
  caption   String?
  category  String   @default("family")
  date      String   @default("2026-07-31") // ISO YYYY-MM-DD (requerido por Gallery/PhotoCard)
  babyAgeDays   Int?
  babyAgeWeeks  Int?
  babyAgeMonths Int?
  babyAgeYears  Int?
  order     Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([category])
}

model Milestone {
  id        String   @id @default(uuid())
  title     String
  date      String
  description String?
  icon      String?
  category  String   @default("milestone")
  order     Int      @default(0)
  location  String?
  parentNote String?
  images    String[] @default([])
  babyAgeDays   Int?
  babyAgeWeeks  Int?
  babyAgeMonths Int?
  babyAgeYears  Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model FamilyMember {
  id          String   @id @default(uuid())
  name        String
  relationship String?
  role        String?
  photoUrl    String?
  quote       String?
  order       Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model GrowthRecord {
  id                String   @id @default(uuid())
  date              String   // ISO YYYY-MM-DD
  babyAgeDays       Int?
  babyAgeWeeks      Int?
  babyAgeMonths     Int?
  babyAgeYears      Int?
  weight            Float?
  height            Float?
  headCircumference Float?
  notes             String?
  createdAt         DateTime @default(now())
}

model Story {
  id        String   @id @default(uuid())
  title     String
  content   String?
  date      String?
  photoUrl  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Setting {
  key   String  @id
  value String?
}
